<!doctype html>
<html lang=en>
<head>
<meta charset=utf-8>
<title>{{ title }}</title>
</head>
<body>

<article>
    <header>
        <h1>{{ title }}</h1>
        <hr />
        <small>{{ date }}</small> | 
        <em>{{ summary }}</em>
        <hr />
    </header>

    <div>
        {{ html }}
    </div>
</article>

</body>
</html>